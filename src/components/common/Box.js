import styled from 'styled-components';
import { color, width, layout, flexbox, space } from 'styled-system';

const Box = styled.div`
  ${color}
  ${width}
  ${space}
  ${flexbox}
  ${layout}
`

export default Box