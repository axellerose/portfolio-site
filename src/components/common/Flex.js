import styled from "styled-components";
import { flexbox, position } from "styled-system";

const Flex = styled.div`
    display: flex;
    ${flexbox}
    ${position}
    
    `

export default Flex;
